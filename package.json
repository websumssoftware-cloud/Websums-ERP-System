{
  "name": "websums-internship-management-system",
  "version": "1.0.0",
  "private": true,
  "description": "Enterprise Internship Management System (MERN Stack) for Websums Software Pvt. Ltd.",
  "scripts": {
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "dev": "concurrently \"npm run dev:client\" \"npm run dev:server\"",
    "build": "npm run build --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
